import SwiftUI

// Settings sub-views — all implemented in TASK 19.

struct GeneralSettingsView: View {
    var body: some View { Text("General — TASK 19") }
}

struct ClipboardSettingsView: View {
    var body: some View { Text("Clipboard — TASK 19") }
}

struct PrivacySettingsView: View {
    var body: some View { Text("Privacy — TASK 19") }
}

struct StorageSettingsView: View {
    var body: some View { Text("Storage — TASK 19") }
}

struct ShortcutSettingsView: View {
    var body: some View { Text("Shortcuts — TASK 19") }
}
